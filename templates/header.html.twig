<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="collapse navbar-collapse" id="navbarSupportedContent">
  
    <ul class="navbar-nav mr-auto">
     

	  {% if path(app.request.attributes.get('_route'),
     app.request.attributes.get('_route_params'))  == '/product/admin/new'%}
      <li class="nav-item active ">
	  {% else %}
	  <li class="nav-item">
	  {% endif %}
	  {% if (app.request.session.get('role') == '1') %}
        <a class="nav-link" href="/product/admin/new/">Add product</a>
		{% endif%}
      </li>
	  {% if (path(app.request.attributes.get('_route'),
     app.request.attributes.get('_route_params')) ) == '/product/list' or (path(app.request.attributes.get('_route'),
     app.request.attributes.get('_route_params')) ) == '/product/admin/list'%}
	  <li class="nav-item active">
	  {% else %}
	  <li class="nav-item">
	  {% endif %}
	  {% if (app.request.session.get('role') == '2') %}
        <a class="nav-link" href="/product/list/">Product List</a>
		{% else %}
		<a class="nav-link" href="/product/admin/list/">Product List</a>
		{%endif%}
      </li>
    </ul>
    <form class="form-inline my-2 my-lg-0">
	<p class="black mr-5 mt-3"> Welcome {{app.session.get('first_name')}} {{ app.session.get('last_name')}}</p>
      <a class="nav-link navbar-nav navbar-light logout" href="/logout" >Logout</a>
    </form>
  </div>
</nav>